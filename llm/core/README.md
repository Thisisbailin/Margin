# LLM Core

Shared types, registry, and error normalization.

当前推荐环境变量（Cloudflare Pages）：
- `QWEN_API_KEY` 或 `DASHSCOPE_API_KEY`
- `LLM_PROVIDER=qwen`
- `LLM_MODEL_L1` / `LLM_MODEL_L2` / `LLM_MODEL_L3`
- `LLM_BASE_URL`（可选，默认 DashScope compatible-mode）
